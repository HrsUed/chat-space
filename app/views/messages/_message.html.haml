/ renderのオプションで部分テンプレートの繰り返し処理をする場合はeach不要。
/ - @messages.each do |message|
.message-header
  .message-header__user-name
    = message.user.name
  .message-header__posted-date
    = message.created_at.in_time_zone("Asia/Tokyo").strftime("%Y/%m/%d %T")
.message-body
  .message-body__content
    - if message.content
      = message.content
  .message-body__image
    - if message.image
      = image_tag message.image
